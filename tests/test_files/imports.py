import os
import sys, \
    argparse
import math as \
    m
from random import randint as rand_int, \
    sample, choice as \
    random_choice


def f(a: float, b: float) -> float:
    from math import sqrt, \
        pow

    return sqrt(a) + pow(b, 5)
